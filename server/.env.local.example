# 本地覆盖项（可选）。复制为 `.env.local`（已 gitignore）。
# `deno task dev` / `deno task start` 会先加载本文件，再加载 `.env`；
# Deno 对同名变量「先出现的生效」，因此这里的值会覆盖 `.env`。
# 部署环境通常只需要一份 `.env`，不必提交 `.env.local`；缺少 `.env.local` 时 Deno 会警告文件不存在，可忽略。

# libSQL（本地文件库示例；远程 Turso 可把 URL + token 写在部署用的 `.env`）
LIBSQL_URL=file:./data/codesmash.db

# 管理员：GitHub 数字 id，逗号分隔（https://api.github.com/user 的 id 字段）
# ADMIN_GITHUB_IDS=
