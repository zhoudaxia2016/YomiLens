# Optional when the UI is not served from the API host (e.g. GitHub Pages).
# No trailing slash. Local dev: leave unset to use same-origin /api (Vite proxy).
# VITE_API_URL=https://codesmash.zhoudaxia2016.deno.net
